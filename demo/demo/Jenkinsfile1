pipeline {
    agent any 
    tools {maven 'maven'}
    
    stages {
        stage('Checkout') {
            steps {
                checkout scm
            }
        }
        
        stage('Build') {
            steps {
                sh 'mvn clean package -f demo/demo/pom.xml'
            }
        }
        
        stage('Test') {
            steps {
                sh 'mvn test -f demo/demo/pom.xml'
            }
        }
    }
}
